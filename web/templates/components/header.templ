package components

templ Header(pageURL string) {
	<div class="mt-5 flex flex-col text-base">
		<div class="flex items-start justify-between align-baseline">
			<nav class="flex space-x-4">
				<a class="font-bold" href="/"><span class="text-blue-700 dark:text-blue-600">Quiz</span> Me</a>
				<a class="hover:text-black dark:hover:text-white font-medium" href="/quizzes">Quizzes</a>
			</nav>
			<div class="flex">
				<button @click="toggleTheme" x-text="'[' + (theme.dark ? 'Dark' : 'Light') + ']'" class="cursor-pointer">[Dark]</button>
			</div>
		</div>
		<div class="h-[1px] mt-3 bg-zinc-600 dark:bg-zinc-700"></div>
	</div>
}
